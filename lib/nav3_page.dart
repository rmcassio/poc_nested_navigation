import 'package:flutter/material.dart';

class Nav3Page extends StatelessWidget {
  const Nav3Page({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Nav3'),
    );
  }
}
