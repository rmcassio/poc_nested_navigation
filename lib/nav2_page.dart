import 'package:flutter/material.dart';

class Nav2Page extends StatelessWidget {
  const Nav2Page({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Nav2'),
    );
  }
}
